# WatchIntervals Agent Instructions

本文件约束所有参与本仓库开发的 AI/编码代理。项目事实以 `docs/README.md` 索引的文档和当前源码为准。

## 开始工作前

1. 阅读 `docs/README.md`。
2. 阅读并执行 `docs/maintenance-workflow.md` 的当次闭环规范。
3. 阅读与任务相关的 `docs/product-requirements.md`、`docs/bugs.md` 和 `docs/testing.md`。
4. 检查 `git status`，保留用户已有的未提交改动。
5. 从源码、可复现行为和已编号文档确认现状，不把临时截图名当成最终事实。
6. 遵守用户在项目上下文中明确给出的代理、模型和工具限制，不调用被禁用的第三方代理。

## 闭环执行

- 默认直接检查、实现、测试和记录；能在当前工作区解决的问题必须在当次开发批次解决。
- 不创建普通 TODO、待办清单、占位实现或无关闭条件的“后续优化”。
- 新发现的 Bug 当次复现、定位根因、修复并增加防复发测试；相同根因复发时更新原 Bug 和原测试。
- 只有用户明确要求延期，或真机、账号、签名、远端状态、关键用户决策等外部条件确实阻断时，才允许保留 `Blocked` 项；必须记录证据、影响和唯一关闭条件。
- 不以计划、分析、编译成功或文档记录代替完成结果。

## 实施约束

- 功能变更关联或新增 `REQ-*`；缺陷修复关联或新增 `BUG-*`。
- 每个有意义的开发批次在 `docs/project-log.md` 记录目标、改动文件、发现/修复的 Bug、根因、防复发测试和验证结果。
- `WorkoutService` 继续作为活动训练状态的唯一所有者。
- 修改数据 schema 时保留向后读取和损坏数据处理。
- 修改传感器融合时明确数据来源、过期条件和切换基线。
- 修改 8765/8766 API 时同步客户端、MCP、开发文档和契约测试。
- 修改手表 UI 时以 378×496 真机布局为基准，检查文字、底部安全区和横纵手势冲突。
- 不提交真实配对码、API Key、Token、精确网络地址、运动轨迹、签名密钥、构建目录、分析 APK 或临时截图。
- APK 作为 GitHub Release 附件发布，不进入 Git 历史。

## 完成条件

1. 目标行为完整实现；当前工作区内可解决的新问题已经闭环，没有遗留半成品。
2. 执行与改动相称的测试；Bug 修复必须增加自动化回归，无法自动化时增加编号人工用例和证据。
3. 代码改动最低执行两个 Android 模块构建和 `git diff --check`。
4. 更新需求、架构、测试、Bug 台账、项目日志、CHANGELOG 中受影响的部分。
5. 在结果中报告实际改动、实际执行的命令和结果、外部条件造成的真机风险及产物路径。
6. 发布时记录版本、提交 SHA、APK SHA-256、构建类型和开放问题。

只改文档时可省略 Android 构建，但仍需验证 Markdown 本地链接和 `git diff --check`。
